# Portal

Screenshots reais do AgênciaHub/Portal do cliente. Nomes preferidos:
`dashboard.png`, `projetos.png`, `aprovacoes.png`, `midia.png`,
`resultados.png` (e `-mobile` para versões mobile). Não inventar telas
que não existem.

## Estado atual (2026-09-13)

**Vazia.** Nenhum screenshot de tela do AgênciaHub ou do Portal do
cliente foi encontrado no repositório `agencia-hub`. O código-fonte das
telas existe (`app/index.html`, `app/js/views/`, `portal/index.html`),
mas nenhuma imagem de tela capturada estava versionada.

Para popular esta pasta: abrir o app (`/app`) e o portal (`/portal`) em
produção ou local, tirar screenshots reais das telas principais, salvar
com a nomenclatura acima e registrar em `../ASSET_INVENTORY.md`.
